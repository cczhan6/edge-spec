name: edge-spec
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytorch::pytorch=2.5.*
  - pytorch::pytorch-cuda=12.4
  - pyyaml
  - pytest
  - pip:
      - transformers>=4.37.0
      - accelerate>=0.30.0
      - safetensors>=0.4.3
      - sentencepiece>=0.2.0
      - socksio>=1.0.0
      - tqdm>=4.66.0
